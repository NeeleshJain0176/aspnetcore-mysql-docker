trigger:
- none

variables:
  myVar1: Prod

pool:
  vmImage: 'ubuntu-latest'

stages:
- stage:
  jobs:
  - job:
    steps:
    - script: echo Testing on Windows $env:DBPWD

# - stage:
#   jobs:
#   - job:
#     steps:
#     - script: echo Testing on Linux!

